mqtt:
  server:
    enabled: ${easy-log.admin.enable}
    port: 1883
    name: Easy-Log-Mqtt-Server
    heartbeat-timeout: 20000
    debug: false
    stat-enable: false
    web-port: 8083

mica:
  redis:
    stream:
      enable: true
      poll-timeout: 1s
      poll-batch-size: 100