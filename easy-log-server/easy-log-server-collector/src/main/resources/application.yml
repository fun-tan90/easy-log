server:
  port: 1134

spring:
  application:
    name: easy-log-server-collector
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  profiles:
    active: test
    group:
      dev:
        - cache
        - el
        - log
      test:
        - cache-test
        - el
        - log